apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "otobo.fullname" . }}-opt
  labels:
    {{- include "otobo.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.accessMode }}
  storageClassName: {{ .Values.persistence.storageClass }}
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
